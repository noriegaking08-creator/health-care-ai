# Health-care


## Deployment

Backend (FastAPI):

```bash
# From the HEALTH CO/ directory
uvicorn api.api:app --reload
```


Frontend (React):

```bash
# From the HEALTH CO/UI/ directory
npm install
npm start
```